# Security — never commit private keys
*.pem
*.key
*.p12
*.pfx

# Build output — not source code
dist/
brixgate-prod.zip

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Claude Code settings
.claude/
.vercel

# Natural Earth source cache for tools/gen-world-dots.py (re-downloads on demand)
tools/ne110.json
